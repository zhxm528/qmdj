# 核心功能
- 前端页面：`/app/bazi/page.tsx`
- 后台程序：`/app/api/bazi/route.ts`
- 步骤1程序：`/app/api/bazi/step1.ts`
- 数据库：`/md/database/2_qimen_pan.sql`
- 配置文件： `/lib/config.ts`
## 需要执行的操作




## 已执行完毕、忽略不执行的操作
- 增加八字对应的五行的"五行分布"和"五行统计"（已完成）
- 根据"五行统计"的结果，用echart实现一个蜘蛛网图表展现统计结果（已完成）
- 修复雷达图标签显示问题：五个点都显示"木2"的问题（已完成，通过使用 indicatorIndex 或通过值匹配来获取正确的索引）
- 修复雷达图标签显示问题：确保每个点显示正确的元素名称和数值（已完成，使用值到索引的映射和精确匹配）
- 修复雷达图标签显示问题：每个点不显示对应的元素名称和数值的问题（已完成，添加了 labelLine、tooltip 和更完善的 label 配置）
- 修复雷达图标签显示问题：当多个元素有相同值时，所有点都显示相同标签的问题（已完成，优先使用 indicatorIndex 而不是值匹配）
- 修复雷达图标签显示问题：当多个元素有相同值时（如土：3、水：3），索引匹配错误的问题（已完成，使用调用顺序作为备用方案，确保即使 indicatorIndex 不可用也能正确匹配）
- 前端在步骤1中调用地支藏干 API 获取地支藏干信息，并以表格形式展示（已完成）



实现的功能
1. 状态管理
添加了 ganMetaData state 用于存储天干五行阴阳表数据
2. API 调用
在 handleBaziPaipan 函数中，获取四柱信息后调用 /api/bazi/wuxingyinyang API
提取四柱的天干（年干、月干、日干、时干）
使用 POST 请求获取对应的天干五行阴阳信息
包含错误处理，失败时仅警告，不影响主流程
3. 表格展示
在步骤1的展示区域添加了"天干五行阴阳表"表格
表格包含4列：柱位、天干、阴阳、五行
显示四柱的天干及其对应的阴阳和五行属性
表格样式与地支藏干表一致
表格位置在天干五行阴阳表之后、地支藏干表之前
4. 数据重置
在开始新的排盘时重置 ganMetaData 为 null
现在在步骤1（定日主）的结果中会显示：
日主信息和五行分布
五行统计雷达图
天干五行阴阳表格（新增）
地支藏干表格

