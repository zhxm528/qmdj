# 核心功能
- 前端页面：`/app/member/page.tsx`
- 后台程序：`/app/api/member/route.ts`
- 数据库表结构： `/md/database/6_member.sql`
- 配置文件： `/lib/config.ts`
## 需要执行的操作
- 读取配置文件中的时区，在写入


## 已执行完毕、忽略不执行的操作
- 页面中“会员等级”板块中的“等级代码”不需要显示
- 页面中“会员基本信息”板块中的“性别”，需要转移，M：男性，F：女性，O：其他
- 页面中“会员基本信息”板块标题改为“基本信息”
- 页面中“会员等级”板块中的“最低积分”和“最高积分”都不需要显示

- 页面中“会员等级”板块中，显示会员有效日期，`member_card`表中的 `expired_at` 格式为yyyy-MM-dd
- 页面中“会员等级”板块中，显示会员注册日期，`member`表中的 `registered_at` 格式为yyyy-MM-dd

- “充值记录”下方增加“消费记录”板块，展示消费记录，对应表 `consumption_transaction`
- 页面中“会员等级”板块中，不显示“会员有效日期”
- 页面中“会员卡”板块中，不显示“卡类型”
- 页面中“会员卡”板块中，“状态”为“正常”的修改显示为“使用中”