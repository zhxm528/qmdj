# 核心功能
## 文件结构
- **前台页面** app/admin/system/users/page.tsx
- **后台程序** app/api/admin/system/users/route.ts
- **后台程序** app/api/admin/system/users/resend_email.ts
- **规则文件** md/app/admin/system/users/cmd.md
- **数据库表** md/database/1_users.sql

## 全局功能
 - 执行该md文件后，把"需要执行的操作"至"已执行完毕、忽略不执行的操作"之间区域的内容，剪切追加至本文件的末尾


## 需要执行的操作

## 已执行完毕、忽略不执行的操作
- 更改样式，"注册邮件" 改为按钮样式渲染显示
- 在页面中的表格列表中，增加一列"操作"，操作列增加"注册邮件"按钮，点击后，调用后台程序 resend_email.ts, 按照注册时的规则重发邮件，注意，这里只是重发邮件，其他动作不用做。
- 查询条件面板支持`收起`和`展开`的功能
- 查询条件中，查询项的标题和查询项的组件是上下结构
- 查询条件的布局是每行4个查询条件，自动换行。
- 实现页面布局：查询条件、已选择的查询条件、表格展示查询结果、翻页组件
- 页面中所有组件采用 Ant Design 的组件渲染展示
- 已选择的查询条件支持删除功能，删除时，需要联带设置清空对应的删除条件
- 翻页组件默认为每页10条，支持选择每页显示的条数，10、100、全部
