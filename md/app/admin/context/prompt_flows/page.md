# 核心功能
- 前端页面：`/app/admin/context/prompt_flows/page.tsx`
- 后台程序：`/app/api/admin/context/prompt_flows/route.ts`
- 数据库表结构：`/md/database/5_context.sql`
- 配置文件： `/lib/config.ts`
## 需要执行的操作




## 已执行完毕、忽略不执行的操作

- 前端页面表格中，调整表格宽度，内容文字尽量不要换行
- 前端页面表格中，表头“操作”一列的宽度需要和表中的内容宽度保持一致
- “流程代码”的提示改为“Flow = 把多个提示词按角色和顺序组织成一次 LLM 调用的“流程配置”，它是提示词管理系统里，从“单条文案”上升到“完整场景能力”的关键抽象。”
- 表格中，去除“ID”一列
- 增加查询条件“流程代码”和“流程名称”的模糊查询
- 新增弹窗，在鼠标点击弹窗外的区域不要关闭弹窗，弹窗中需要增加"关闭"的按钮，用来点击关闭弹窗
- 点击"编辑"按钮时，查询相关记录并回显在编辑弹窗中
- 在后台日志中打印查询的 SQL 语句
- 避免编辑弹窗数据回显问题：使用 useEffect 监听 Modal 打开状态，确保在 Modal 完全打开后再设置表单值
- 点击"编辑"按钮时，在日志中详细打印后端返回给前端的完整内容

- 在表格的“操作”一列，增加一个按钮“复制”，实现复制“流程”的功能，点击“复制”按钮需要先弹出一个框，提示信息：“复制流程，同时也复制流程下的所有步骤”；
- 复制“流程”的功能包括：复制“流程”、复制“流程步骤”；




- 功能说明：
点击流程列表中的“流程名称”，会在新页面打开流程步骤页面
流程步骤页面会自动根据 URL 参数中的 flow_id 过滤显示该流程下的所有步骤
查询条件中的“流程”下拉框会自动设置为对应的流程


