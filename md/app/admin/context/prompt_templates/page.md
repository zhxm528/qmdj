# 核心功能
- 前端页面：`/app/admin/context/prompt_templates/page.tsx`
- 后台程序：`/app/api/admin/context/prompt_templates/route.ts`
- 数据库表结构：`/md/database/5_context.sql`
- 配置文件： `/lib/config.ts`
## 需要执行的操作
- 新增表单中的“当前版本ID”改为下拉框选项 查询 `prompt_template_versions` 表


## 已执行完毕、忽略不执行的操作

- 新增弹窗，在鼠标点击弹窗外的区域不要关闭弹窗，弹窗中需要增加"关闭"的按钮，用来点击关闭弹窗
- 点击"编辑"按钮时，查询相关记录并回显在编辑弹窗中
- 在后台日志中打印查询的 SQL 语句
- 避免编辑弹窗数据回显问题：使用 useEffect 监听 Modal 打开状态，确保在 Modal 完全打开后再设置表单值
- 点击"编辑"按钮时，在日志中详细打印后端返回给前端的完整内容
- 前端页面表格中，调整表格宽度，内容文字尽量不要换行
- 前端页面表格中，表头“操作”一列的宽度需要和表中的内容宽度保持一致
- 表格中，去除“ID”一列
- 表格中，去除“创建者”一列
- 增加查询条件“模板”的输入框，支持模糊查询
- 增加查询条件“状态”的下拉框查询


- 增加查询条件“模板” 改为 “逻辑键”
- 增加查询条件“场景代码”的输入框，支持模糊查询
- 增加查询条件“作用范围”的输入框，支持模糊查询
- 增加查询条件“模板”的输入框，支持模糊查询
- 增加查询条件“角色”的下拉框查询
- 增加查询条件“项目”的下拉框查询
- 增加查询条件“语言”的下拉框查询


- 增加查询条件“作用范围”改为下拉框选项
- 新增表单中，在每个标题后增加一个输入说明，例如：“（20个字符以内）”这样的文字说明