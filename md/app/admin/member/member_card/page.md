# 核心功能
- 前端页面：`/app/admin/member/member_card/page.tsx`
- 后台程序：`/app/api/admin/member/member_card/route.ts`
- 数据库表结构：`/md/database/6_member.sql`
- 配置文件： `/lib/config.ts`
## 需要执行的操作
- ~~新增卡时，卡号是系统自动生成的，用户不可修改，卡号规则为：'QM'+日期yyyyMMddHHmmss+4位随机数~~ ✅ 已完成



## 已执行完毕、忽略不执行的操作
- 前端页面表格中，增加“发卡”操作按钮，点击后，弹窗选择发卡类型： 表 `membership_level`
- 选择卡类型后，提交保存，则修改发卡日期和有效日期，发卡日期为当前日期，有效日期为31天

- 前端页面表格中，“状态”显示为“正常”的改写为“使用中”

- 前端页面表格中，“发卡”操作按钮，点击后，弹窗中的待选择的发卡类型从表 `membership_level`查询；
- 提示语不需要显示
- 卡类型选择组件改为Ant design的Radio单选组件
- 前端页面表格中，“状态”显示为“正常”的改写为“使用中”
- 前端页面表格中，“发卡”操作按钮，点击后，弹窗中显示空白，需要查看原因
- 前端页面表格中，表头需要和表中的内容宽度保持一致，操作一列宽度是否不一致？
- 选择卡类型后，提交保存，读取配置文件中的时区，修改发卡日期和有效日期
- 发卡的弹窗中，默认选择当前用户所属的会员等级
- 提交“发卡”时，需要保存 `member`表中的`level_id`
- 列表中，点击“编辑”按钮，需要在编辑页面回显当前选中的记录