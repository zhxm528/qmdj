## 核心功能
- 前端页面：`/app/admin/member/member/page.tsx`
- 后台程序：`/app/api/admin/member/member/route.ts`
- 数据库表结构：`/md/database/6_member.sql`
- 配置文件： `/lib/config.ts`
## 需要执行的操作

## 已执行完毕、忽略不执行的操作
- ✅ 列表中，性别转移 F：女性 O：其他 M：男性
- ✅ 列表中，"等级ID"改为"等级名称"
- ✅ 编辑弹框中查询选中行的记录，并回显在页面上
- ✅ 新增弹窗，在鼠标点击弹窗外的区域不要关闭弹窗，弹窗中需要增加"关闭"的按钮，用来点击关闭弹窗
- ✅ 列表表头中"操作"一列宽度要和表格内容宽度一致
- ✅ 点击"编辑"按钮时，查询相关记录并回显在编辑弹窗中
- ✅ 在后台日志中打印查询的 SQL 语句
- ✅ 修复编辑弹窗数据回显问题：使用 useEffect 监听 Modal 打开状态，确保在 Modal 完全打开后再设置表单值
- ✅ 点击"编辑"按钮时，在日志中详细打印后端返回给前端的完整内容
