# 核心功能
- 账户信息前端页面：`/app/account/page.tsx`
- 后台程序：`/app/api/account/route.ts`
- 会员信息前端页面：`/app/member/page.tsx`
- 后台程序：`/app/api/member/route.ts`
- 数据库表结构: `/md/database/6_member.sql`
## 需要执行的操作
- "会员卡有效期" 的日期显示，也需要像"会员等级"一样有背景高亮的渲染方式显示~~ ✅ 已完成








## 已执行完毕、忽略不执行的操作
- 前端页面：`/app/account/page.tsx`
- 后台程序：`/app/api/account/route.ts`

- 调用后台程序的连接 从 `/api/user/me` 改为 `/app/api/account/route.ts`
- 后台程序：`/app/api/account/route.ts` 中 实现 获取用户信息，并回显到前台页面上
- 前端页面：`/app/account/page.tsx` 上只显示 users 表中的信息
- 前台页面上实现一个按钮 “查看账户” 跳转到新的前台页面：`/app/member/page.tsx` , 在页面中显示用户的会员信息，第一次访问发现用户还不是会员，则自动创建会员的相关信息
- 会员信息的相关表参考： /md/database/member/*.md , 实现用户查询自己的会员账户相关信息


- 实现充值的逻辑，并显示充值记录
- 点击“充值”按钮，输入一个金额，点击“保存”完成充值。
- 1元=1个积分
- 提示充值成功的弹窗，用ant design的弹窗组件，显示在屏幕的中间
- “1元 = 1积分”字不需要显示
- "充值记录" 增加翻页功能，每页显示10条记录

- “会员基本信息”中不显示“会员ID”、“会员编码”、“状态”、“更新时间”

- 规则：QM + yyyyMMddHHmmss（14位时间戳）+ 4位随机数字
- 实现细节
- 格式：QM + 年月日时分秒（14位）+ 4位随机数字
- 示例：QM202512121430451234（QM + 2025年12月12日14时30分45秒 + 随机数1234）
- 总长度：20 位字符
- "会员基本信息" 添加“编辑”按钮，支持修改“会员基本信息”中的属性

- 修改“会员基本信息”中的属性时，需要判断“手机号”的唯一性和“姓名”的唯一性。
- “账户信息”中需要显示会员等级
- 页面初始加载判断已经注册的会员是否属于某一等级的会员，如果没有会员等级则默认设置一个最低的等级，即“白银会员”，并插入相应的表；

- “账户信息”中需要显示会员等级，“银卡”改为“白银会员”
- “会员卡有效期”对应的是表 `member_card` 中的 `expired_at` 字段



- 功能说明：
点击“修改密码”按钮会弹出修改密码弹窗
用户需要输入新密码和确认密码
如果两次输入的密码不一致，会显示错误提示
如果密码长度少于6位，会显示错误提示
密码修改成功后，会显示成功提示并关闭弹窗


- 修改密码弹窗中的按钮：
"取消"按钮：白色背景，灰色文字，灰色边框（1px solid #d9d9d9）
"确定"按钮：蓝色背景
两个按钮之间有适当间隔（通过 gap-3 实现）

- “修改密码”的弹窗中，"取消"按钮的灰色边框还是白色，需要调整为灰色